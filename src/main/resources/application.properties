spring.application.name=main

# Server Configuration
server.port=2345

# Database Configuration - SQL Authentication (Simplest approach)
spring.datasource.url=jdbc:sqlserver://localhost:1433;databaseName=computershop;encrypt=true;trustServerCertificate=true
spring.datasource.username=hai
spring.datasource.password=hai
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.SQLServerDialect

# Thymeleaf Configuration
spring.thymeleaf.mode=HTML
spring.thymeleaf.encoding=UTF-8
spring.thymeleaf.servlet.content-type=text/html
spring.thymeleaf.cache=false

# Static Resources
spring.web.resources.static-locations=classpath:/static/

# Server Configuration
server.port=2345
server.servlet.context-path=/

# Session Configuration
server.servlet.session.timeout=30m
server.servlet.session.cookie.max-age=1800

# Payment - MoMo (Pay-to-phone QR via deep link)
payment.momo.receiver-phone=0983580550